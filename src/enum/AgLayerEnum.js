import tileTypeEnum from "./tileTypeEnum";
import defaultLayerEnum from "./defaultLayerEnum";

/**
 * agLayer所有用到的enum
 */
class AgLayerEnum{
    constructor() {
        this.tileTypeEnum = tileTypeEnum;
        this.defaultLayerEnum = defaultLayerEnum;
    }
}

export default AgLayerEnum;
